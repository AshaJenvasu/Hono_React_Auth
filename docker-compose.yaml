services:
  postgres:
    image: postgres:18.6
    restart: unless-stopped
    container_name: hono-todos
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_PASSWORD= password
      - POSTGRES_USER= user
      - POSTGRES_DB= todos
